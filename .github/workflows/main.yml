name: Docker Image CI

on: 
  push:
    branches: [main]

jobs:

  build-container:
    runs-on: self-hosted
    steps:
      - name: Build the docker image
        run: docker build . --file Docker file --tag teste2:teste2
